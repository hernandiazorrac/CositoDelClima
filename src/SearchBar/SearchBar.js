import './SearchBar.css'

const SearchBar = () => {
return(
    <div className="searchBox">
      <input type="text" className="searchBar" placeholder="Buscar ciudad..." />
    </div>
)
}

export default SearchBar;